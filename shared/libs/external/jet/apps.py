from django.apps import AppConfig


class JetAppConfig(AppConfig):
    name = 'shared.libs.external.jet'  # Set your desired app label here
    verbose_name = 'Jet App'  # Optional: Set a human-readable name for the app
